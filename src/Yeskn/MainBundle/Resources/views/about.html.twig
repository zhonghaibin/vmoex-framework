{% extends '@YesknMain/base.html.twig' %}

{% block main %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <i class="fa fa-file-text"></i> {{ 'about_site'|trans }}
                </div>
                <div class="panel-body">
                    {% set locale = app.request.locale %}
                    {% if locale == 'en' %}
                        {{ row.en|raw }}
                    {% elseif locale == 'jp'  %}
                        {{ row.jp|raw }}
                    {% elseif locale == 'zh_TW' %}
                        {{ row.zhTW|raw }}
                    {% else %}
                        {{ row.zhCN|raw }}
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
